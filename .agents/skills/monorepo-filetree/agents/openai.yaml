interface:
  display_name: "Monorepo File Tree"
  short_description: "Generate root and app file tree docs"
  default_prompt: "Use $monorepo-filetree to regenerate monorepo-filetree.md, each apps/*/docs/filetree.json and apps/*/docs/filetree.html, and the root AGENTS.md file tree guidance."

policy:
  allow_implicit_invocation: false
